import { mkdir, writeFile } from "node:fs/promises";
import { dirname, resolve } from "node:path";

const DEFAULT_CONFIG_URL = "https://qxp.kisakay.com/";

function argValue(name) {
  const index = process.argv.indexOf(name);
  return index >= 0 ? process.argv[index + 1] : "";
}

function httpOrigin(value) {
  const text = String(value || "").trim().replace(/\/+$/, "");
  if (!text) return "";

  const url = new URL(text.includes("://") ? text : `https://${text}`);
  if (url.protocol !== "http:" && url.protocol !== "https:") {
    throw new Error(`Unsupported server origin protocol: ${url.protocol}`);
  }
  return url.origin;
}

function webSocketUrlFromOrigin(origin) {
  const url = new URL(origin);
  url.protocol = url.protocol === "https:" ? "wss:" : "ws:";
  url.pathname = "/ws";
  url.search = "";
  url.hash = "";
  return url.toString();
}

function extractRuntimeConfig(html) {
  const match = String(html || "").match(
    /<script\b[^>]*>\s*window\.__QXP_RUNTIME__\s*=\s*(\{[\s\S]*?\})\s*;?\s*<\/script>/m
  );
  if (!match) {
    throw new Error("Could not find window.__QXP_RUNTIME__ in the production HTML.");
  }
  return JSON.parse(match[1]);
}

const configUrl = argValue("--url") || process.env.QXP_RUNTIME_CONFIG_URL || DEFAULT_CONFIG_URL;
const outputPath = resolve(argValue("--out") || "dist/runtime-config.js");
const serverOrigin = httpOrigin(
  argValue("--server-origin") || process.env.QXP_SERVER_ORIGIN || new URL(configUrl).origin
);

const response = await fetch(configUrl);
if (!response.ok) {
  throw new Error(`Could not fetch ${configUrl}: HTTP ${response.status}`);
}

const runtimeConfig = extractRuntimeConfig(await response.text());
const payload = {
  ...runtimeConfig,
  serverOrigin,
  apiBaseUrl: runtimeConfig.apiBaseUrl || serverOrigin,
  wsUrl: runtimeConfig.wsUrl || webSocketUrlFromOrigin(serverOrigin)
};

await mkdir(dirname(outputPath), { recursive: true });
await writeFile(
  outputPath,
  `// Generated by scripts/sync-runtime-config.mjs. Do not edit by hand.\nwindow.__QXP_RUNTIME__ = ${JSON.stringify(payload)};\n`,
  "utf8"
);

const rtcStatus = payload.rtc?.callsEnabled ? "enabled" : "disabled";
console.log(`Wrote ${outputPath} from ${configUrl} with RTC ${rtcStatus}.`);
